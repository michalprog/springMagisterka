services:
  server:
    build:
      context: .
    ports:
      - "8080:8080"
    depends_on:
      - postgres
      - mongo

  postgres:
    image: postgres:15
    #pgvector/pgvector:pg16
    #postgres:15-alpine
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "TjtPO93RGHcX4KPTtf8kjyu9kYnjOtAG"
      POSTGRES_DB: spring_magisterka
    ports:
      - "8090:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  mongo:
    image: mongo:6
    container_name: mongo_db
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  postgres_data:
  mongo_data: